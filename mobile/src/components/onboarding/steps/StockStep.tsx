/** Placeholder step — replaced with the real implementation in a later task. */
import { StepScaffold } from '../StepScaffold';

export function StockStep() {
  return <StepScaffold title="Stock" subtitle="Configurez vos articles." />;
}
