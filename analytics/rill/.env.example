# Rill design-time connection to the dev Postgres (see backend application-dev.yml).
# Copy to .env and fill the dev DB password (local dev default lives in application-dev.yml).
avicare_postgres_dsn=postgres://avicare:<DEV_DB_PASSWORD>@localhost:5434/avicare?sslmode=disable
